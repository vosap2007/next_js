import { FC } from 'react';
import Heading from './Heading';

const Footer: FC = () => {
  return (
    <footer>
      <Heading text={'Footer'} tag={'h3'} />
    </footer>
  );
};

export default Footer;
